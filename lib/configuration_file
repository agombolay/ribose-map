#Optional Parameters
pattern: UMI/barcode pattern; N=random base, X=fixed base (pattern='NNNNNNXXXNN')
barcode: Nucleotide sequence of molecular barcode present in reads (barcode='TCA')

#Required Parameters
sample: Name of sample to use for labeling output files (sample='sample1')
technique: Name of rNMP sequencing technique used (technique='ribose-seq')

repository: Path to output repository (repository='/filepath/output')

read1: Path to FASTQ file of sequencing read 1 (read1='/filepath/sample1_1.fastq')
read2: Path to FASTQ file of sequencing read 2 (read1='/filepath/sample1_2.fastq')

fasta: FASTA file of reference genome sequence (fasta='/filepath/sacCer2.fa')
basename: Basename of Bowtie2 genome index files (basename='/filepath/sacCer2')

seed: Seed size to use for Bowtie2 alignment (seed='123456789')
threads: Number of threads to use for Bowtie2 and SAMtools (threads='4')
quality: Minimum quality score cut-off for filtering reads (quality='0')

#Required if technique='emRiboSeq, HydEn-seq, or Pu-seq'
check: Check and remove coordinates from BED file that exceed length of chromosome (check='yes')

mito: Name of mitochondrial sequence in FASTA file (mito='chrM')
other: Name of sequences in FASTA file other than nuclear and mito (other='chloroplast plasmid')

scale: Use same scale for all plots produced by Distribution Module (scale='no')
